#include <stdio.h>

/**
 * main - this code prints the name of the file
 * it was compiled from
 *
 * Return: Always 0 whenit is a (Success)
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
