#include "main.h"

/**
* _abs -this one will computate the exact value of an integer
* @n: the int to check
*
* Return: the absolute value of int
*/
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
